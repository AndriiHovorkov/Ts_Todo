import  { makeStyles } from '@mui/styles';

export const todoListStyle = makeStyles(theme => ({
    container: {
        width: 900,
        border: '1px black solid',
        marginLeft: 'auto',
        marginRight: 'auto'
    }
}));