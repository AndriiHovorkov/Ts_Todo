import  { makeStyles } from '@mui/styles';

export const cardStyles = makeStyles(theme => ({
    cont: {
        marginTop: 50,
        width: 1200,
        marginLeft: 'auto',
        marginRight: 'auto'
    }
}));